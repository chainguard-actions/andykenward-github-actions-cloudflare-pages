name: Test github-environment input - delete deployments for specific environment
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          cloudflare-api-token: fake-cf-token
          github-token: ${{ github.token }}
          github-environment: 'Preview'
      - name: Assert action ran and failed gracefully with github-environment set
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: action succeeded with github-environment=Preview"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (expected - sentinel repo or invalid CF token)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
